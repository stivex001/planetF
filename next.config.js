/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["via.placeholder.com", "images.pexels.com", "avatars.githubusercontent.com"],
      },
}

module.exports = nextConfig
